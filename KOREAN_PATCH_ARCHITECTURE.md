# 쿠니오 시대극 한국어 패치 아키텍처 결정

상태: 재설계 확정, 동적 CHR 페이지는 보류

## 기준

영어 패치는 한국어 문장이나 코드를 복사하는 대상이 아니다. 다음 구조만
참고한다.

- 기준 일본 ROM MD5는 `0d406a85285b4de8468f0dab6aad5fe5`다.
- Bank 1의 `0x05DD4-0x05FC3`에는 248개 대사 포인터가 있다.
- 영어 패치는 244개 포인터를 바꿔 길어진 레코드를 Bank 1 안에서 재배치했다.
- 영어 대사 글꼴은 물리 CHR Bank 7을 사용한다.
- 영어 IPS는 ROM 확장이나 범용 페이지 전환을 증명하지 않는다.

세부 근거는 `rom_analysis/english_patch_implementation_map.md`에 둔다.

## 확인된 안정 경로

오프닝 pointer 182는 고정된 920프레임 경로에서 frame 883에 확인할 수 있다.

- 레코드: ROM `0x071B6`, CPU `B1A6`
- 렌더러: 16x16 한글 타일 쌍을 실제 대사 큐에 출력한다.
- 화면 문맥: MMC3 control `0x07`, R0/R1 `0x3C/0x3E`, PPUCTRL `0x88` 또는
  `0x8C`, 배경 패턴 테이블 `0x0000`
- 검증: 기존 Bank 7 후보는 출력 타일 75개가 변경 CHR 타일과 모두 일치한다.

따라서 첫 번역 배치는 이 **고정 Bank 7 경로**만 사용한다. 즉, 같은 글꼴
페이지로 표현할 수 있는 소수의 오프닝 대사부터 카탈로그 기반으로 만들고,
각 후보는 대상 레코드 읽기와 고정 화면 캡처를 확인한다.

## 보류한 동적 페이지 전환

두 후보를 만들고 같은 고정 경로로 검사했다.

| 후보 | 정적 빌드 | 대상 레코드 | 최종 MMC3/화면 | 판정 |
| --- | --- | --- | --- | --- |
| `bank8_page_switch_proof` | PASS | PASS, 18/18 읽기 | R0/R1가 다시 `3C/3E`로 복귀 | FAIL: 페이지 값이 지속되지 않음 |
| `bank8_persistent_page_proof` | PASS | FAIL, 0/37 읽기 | frame 883 검은 화면 | FAIL: 고정 PRG 코드에서 Bank 1 코드 동굴로 점프할 수 없음 |

첫 후보는 `0x0502/0x0503`에 `0x40/0x42`를 써서 Bank 8을 잠깐 선택했지만,
게임의 프레임별 MMC3 갱신이 곧 원래 값으로 덮었다. 두 번째 후보는 그 갱신을
후킹했지만, CPU `FEDD`는 고정 PRG 코드이고 `BFA5`는 가변 Bank 1 창이다.
그 시점에 Bank 1이 매핑된다는 보장이 없어서 코드가 아닌 데이터를 실행했다.

이 두 후보와 관련 ROM/IPS는 릴리스 후보가 아니다. 소스, 로그, 작은 PNG만
증적으로 보존하며 생성 ROM과 원시 덤프는 Git에 넣지 않는다.

## 새 작업 순서

1. **대사 카탈로그를 먼저 완성한다.** 248개 포인터는 레코드 범위, 제어 토큰,
   번역 상태, 글꼴 페이지 요구량을 갖는다. 일본어처럼 보이는 바이트는
   포인터/문맥이 확인될 때까지 번역 대상으로 승격하지 않는다.
2. **고정 Bank 7 파일럿을 만든다.** pointer 182와 동일한 렌더러 계열에서,
   같은 글꼴 팔레트로 표현 가능한 오프닝 2개 이하를 선택한다. 길어진 레코드는
   영어 패치가 증명한 Bank 1 포인터 재배치 규칙을 사용한다.
3. **파일럿마다 짧게 확인한다.** 대상 Lua, 최대 920프레임, 명시적인
   `lua_done`, 대상 읽기, 화면 캡처를 필수로 한다. 게임 이동, 전투, 보스,
   자유 autoplay는 이 단계에 넣지 않는다.
4. **동적 페이지 전환은 별도 연구 트랙으로 둔다.** 먼저 마지막 고정 PRG
   뱅크 안의 재배치 가능한 코드 공간 또는 안전한 코드 재배치 방법을 증명한다.
   그 뒤 무해한 실행 마커, 페이지 활성화/해제 수명, 두 장면의 CHR 충돌 검사를
   순서대로 통과해야 한다.
5. **그 이후에만 넓은 한글 글꼴을 설계한다.** 고정 페이지의 글자 수가 부족한
   것이 확인되면, 동적 페이지 또는 렌더러 확장/조합형 한글을 비교한다. 현재
   실패한 Bank 8 훅을 재사용하거나 우회하지 않는다.

## 후보 판정

- `PASS`: 범위 검사, 부팅, 대상 레코드, mapper/CHR 문맥, 네이티브 화면이 모두
  맞는다.
- `FAIL`: 이번처럼 페이지 값 복귀, 잘못된 PRG 창 실행, 대상 레코드 손실 등
  원인이 확인됐다.
- `UNKNOWN`: 정적 빌드는 됐지만 대상 화면/문맥 증거가 없다.

릴리스 후보에는 PASS만 넣는다. 개발 중에는 FAIL/UNKNOWN 후보도 결과와 원인을
남기되, 다음 번역 배치에 자동으로 포함하지 않는다.
