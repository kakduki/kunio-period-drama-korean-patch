# 쿠니오 시대극 한국어 패치 재시작 계획

상태: 현재 작업 기준

## 결론

게임을 처음부터 자동으로 플레이하면서 대사 화면을 찾는 방식은 한국어 패치
작업의 중심 전략으로 사용하지 않는다. 이 게임은 졸병 처리, 보스 등장, 이벤트
진행이 결합되어 있어 무목적 입력은 첫 화면이나 메뉴에서 반복되기 쉽다.

대신 완성된 영어 패치가 이미 보여 준 구조를 정적으로 복원한다. 즉, 대사
포인터, 레코드 형식, 제어 바이트, 글자 코드, CHR 글꼴, 긴 레코드의 재배치
규칙을 먼저 도구화하고, 그 뒤에 한글을 장면 단위로 넣는다. FCEUX는 구조를
찾는 용도가 아니라 이미 특정한 후보의 화면을 짧게 확인하는 용도로만 쓴다.

## 영어 패치에서 확인한 작업 기준

영어 IPS는 저장소에 넣지 않고 메모리에서만 분석한다. 영어 문장은 한국어
번역의 원문으로 쓰지 않으며, 아래의 구현 구조만 참고한다.

| 확인 항목 | 영어 패치에서 확인한 사실 | 한국어 작업에서의 의미 |
| --- | --- | --- |
| 기준 ROM | MD5 0d406a85285b4de8468f0dab6aad5fe5 | 모든 빌드와 분석의 기준 파일을 고정한다. |
| 주요 대사 포인터 | ROM 0x05DD4-0x05FC3, 248개 엔트리 | 대사 레코드의 소유자와 재배치 대상을 포인터 단위로 관리한다. |
| 주요 대사 블록 | Bank 1의 0x05DD4-0x07766 | 한국어 대사 카탈로그의 첫 대상이며, 문자열 탐색 결과를 무작정 패치하지 않는다. |
| 영어 글자 코드 | 0x81-0x9A가 A-Z에 대응 | 한글 코드/타일 할당도 대사 렌더러의 별도 경로로 설계해야 한다. |
| 대사 글꼴 | 물리 CHR Bank 7의 0x181-0x19A가 확인됨 | 화면별 CHR 매핑을 확인한 뒤에만 한글 타일을 덮는다. |
| 긴 문장 처리 | 포인터를 바꾸고 Bank 1의 다른 공간으로 레코드를 이동 | 고정 길이 덮어쓰기가 아니라 레코드별 포인터 재계산과 소유 범위 검사가 필요하다. |
| 다른 텍스트 계열 | 이름표, 포인터 이전 텍스트, 성장 UI, 메뉴/라벨도 별도 변경 | 메뉴, 상태, 아이템은 대사 결과를 그대로 상속하지 않는다. |

세부 근거는 rom_analysis/english_patch_implementation_map.md,
rom_analysis/english_patch_reference.md, rom_analysis/english_script_reference.md에
기록한다.

## 작업 원칙

1. 대상 레코드, 종료 조건, 최대 프레임이 없는 FCEUX 실행은 금지한다.
2. 일반 autoplay Lua는 기본 실행을 거부한다. 진단 목적으로 명시 허용해도
   900프레임과 45초를 넘지 못한다.
3. 일본판 바이트가 문자열처럼 보인다는 사실만으로 번역하거나 패치하지 않는다.
   포인터, 컨텍스트, 제어 토큰, 종료 규칙을 함께 기록한다.
4. 영어 IPS의 문장, 코드, 헤더 변경을 복사하지 않는다. 구조 정보만 참고한다.
5. 한글 글자가 화면에 보였다는 이유만으로 글꼴을 완료 처리하지 않는다.
   실제 크기 화면에서 읽기 쉬워야 하며, 필요한 글자 수를 감당할 구조여야 한다.
6. 생성 ROM, IPS, 원시 덤프는 로컬에만 둔다. Git에는 소스 코드, 카탈로그,
   리포트, 작은 PNG 증거만 넣는다.
7. 화면 증거가 없으면 PASS가 아니라 UNKNOWN이다. UNKNOWN은 실패가 아니지만
   다음 배치에 자동 승격되지 않는다.

## 현재 증거의 위치

오프닝 포인터 182는 한글 16x16 렌더링, 47바이트 레코드, 인접 포인터 보존,
화자명 뒤의 눈에 보이는 콜론을 한 화면에서 증명했다. 이 결과는 대사
렌더러의 기술 증거다. 전체 글꼴, 전체 대사, 메뉴/상태 렌더러, 완성 릴리스의
증거는 아니다.

- 기준 레코드: ROM 0x071B6, CPU B1A6, pointer 182
- 현재 화면 문구: 쿠니마사: 어서 움직여! / 분조 두목이 큰일이야!
- 런타임 결과: 47/47 대상 읽기 일치, frame 883, lua_done
- 실제 출력 타일: 75개 타일 출력이 모두 후보 ROM의 변경 CHR 타일과 일치
- 화면별 CHR 상태: MMC3 control 0x07, R0/R1=0x3C/0x3E, PPUCTRL=0x88 또는
  0x8C. 두 PPUCTRL 값 모두 배경 패턴 테이블 0x0000을 사용한다.
- 이 상태에서는 MMC3의 R0/R1 2KiB 쌍 때문에 대사 출력 코드 0x00-0xFF가
  물리 CHR Bank 7의 tile 0x100+code에 대응한다. 이 공식은 pointer 182의
  검증된 오프닝 문맥에만 적용한다.
- 인접 pointer 183: 정적 보존 PASS, 실제 화면은 UNKNOWN

이 증거는 rom_analysis/opening_dialogue_16x16_speaker_separator_proof.md와
rom_analysis/dialogue_glyph_capacity_plan.md,
rom_analysis/opening_mapper_trace_capture/analysis.md,
rom_analysis/opening_font_runtime_mapping_audit.md에 남긴다.

## 단계 0: 실행 안전장치

목표: 첫 화면을 무한 반복하는 실행 경로를 작업 흐름에서 제거한다.

- FCEUX 실행기는 전용 Lua 스크립트를 명시하지 않으면 시작하지 않는다.
- legacy autoplay는 명시 허용 없이 거부하고, 허용된 경우에도 짧은 진단으로만
  실행한다.
- 모든 전용 Lua는 대상 바이트, 화면 캡처 시점, lua_done 또는 hit_limit 같은
  종료 신호를 가져야 한다.
- 테스트는 실행기를 실제 GUI로 열지 않고도 이 정책을 확인한다.

완료 조건: 무타깃 명령이 에뮬레이터를 열지 않고 실패하며, 타깃 Lua는
프레임/시간 상한과 종료 신호를 기록한다.

## 단계 1: 정적 대본 모델

목표: 번역 전에 대사 데이터의 소유 관계와 형식을 재현 가능하게 만든다.

1. 영어 패치의 248개 포인터 엔트리를 일본판 엔트리와 인덱스별로 고정한다.
2. 각 일본판 레코드에서 원본 바이트와 제어 토큰을 그대로 보존한다.
3. 레코드를 대사, 이름표, 포인터 이전 텍스트, 성장 UI, 메뉴/라벨로 분류한다.
4. 일본어 글자 코드의 해석이 불명확한 경우에는 토큰 그대로 보존하고
   needs_context로 남긴다.
5. 영상과 전사는 장면 순서와 일본어 의미를 확인하는 보조 자료로만 사용한다.

카탈로그 필수 열:

id, renderer_family, context, pointer_index, pointer_offset, jp_offset,
jp_bytes, control_tokens, english_structure_reference, korean_text, status

완료 조건: 주요 대사 포인터 248개는 모두 소유자와 대상 오프셋을 갖고,
번역 후보는 원문과 제어 토큰을 잃지 않는다.

## 단계 2: 릴리스 가능한 한글 글꼴 설계

목표: 장면 하나에만 맞춘 글자 묶음이 아니라, 여러 대사를 빌드할 수 있는
글꼴/코드/CHR 전략을 증명한다.

현재 16x16 방식은 한글 한 글자를 여러 8x8 타일로 구성한다. 따라서 전체
한글 음절을 한 CHR 뱅크에 모두 넣는다고 가정하면 안 된다. 다음 세 방식을
증거로 비교한 뒤 하나를 선택한다.

1. 장면 또는 대사 묶음별 CHR 글꼴 페이지: 동시에 필요한 글자만 한 페이지에
   넣고, 실제 게임의 뱅크 전환 시점과 충돌하지 않는지 확인한다. Bank 7 복제
   페이지의 첫 두 실험은 페이지 값 복귀와 가변 PRG 창 실행 문제로 FAIL했다.
   따라서 고정 PRG 코드 공간과 페이지 수명 제어가 별도로 증명되기 전에는 이
   방식을 번역 후보에 사용하지 않는다.
2. 대사 코드/렌더러 확장: 제어 바이트를 보존하면서 더 넓은 소스 코드 풀을
   해석하도록 렌더러를 확장한다.
3. 조합형 한글: 초성/중성/종성을 조합해 타일 수를 줄이는 방법이다. 앞의 두
   방식이 용량과 안정성 요구를 만족하지 못할 때만 검토한다.

각 방식은 다음을 모두 기록해야 한다.

- 사용 가능한 코드와 예약된 제어 바이트
- 각 코드가 가리키는 실제 CHR 타일과 화면별 뱅크 상태
- 한 화면에서 동시에 필요한 글자 수
- 타일 충돌 여부와 네이티브 크기 가독성
- 코드 변경과 CHR 변경의 바이트 허용 목록

완료 조건: 적어도 하나의 다중 레코드 대사 묶음이 필요한 모든 글자를
정적으로 할당할 수 있고, 같은 글꼴 페이지의 서로 다른 두 레코드가 실제
화면에서 읽힌다.

## 단계 3: 카탈로그 기반 컴파일러

목표: 수동 헥스 편집 없이 같은 입력에서 같은 후보 ROM을 만든다.

- 한국어 문장과 제어 토큰을 분리한 JSON/TSV 입력을 사용한다.
- 글꼴 페이지와 코드 배정을 입력으로 받아 바이트 스트림을 만든다.
- 길어진 레코드는 선언된 빈 공간에만 배치하고 포인터를 재계산한다.
- 원래 레코드를 옮길 때는 참조하는 인접 포인터와 원문 보존을 검사한다.
- 변경 허용 범위는 텍스트, 포인터, 승인된 코드, 승인된 CHR 타일로 제한한다.
- 빌드 결과는 기준 ROM의 해시, 변경 목록, 포인터 이동, 글꼴 페이지, 부팅
  스모크 결과를 보고한다.

완료 조건: 수정하지 않은 레코드는 round-trip에서 원본 바이트와 같고, 수정한
레코드는 지정한 오프셋/포인터/CHR 범위 밖을 바꾸지 않는다.

## 단계 4: 장면별 작은 배치

전체 대사를 한 번에 번역하지 않는다. 아래 순서를 따른다.

| 순서 | 대상 | 이유 | 검증 방식 |
| --- | --- | --- | --- |
| 1 | 오프닝 대사 2-5개 | 대사 포인터와 16x16 경로가 이미 부분 증명됨 | frame 883 같은 고정 루트 또는 저장 상태 |
| 2 | 제목/메뉴 | 즉시 접근 가능하지만 대사와 다른 렌더러일 수 있음 | 짧은 메뉴 입력 루트 |
| 3 | 상태/아이템/상점 | 자주 보이는 UI이며 폭 제약이 큼 | 저장 상태, 메뉴 루트, 대상 읽기 |
| 4 | 이벤트/보스 대사 | 게임 진행 조건이 복잡함 | 검증된 세이브 상태, 디버그 상태, 제한된 치트 |
| 5 | 나머지 대사 | 앞 단계에서 정해진 글꼴/컴파일러를 사용 | 장면별 회귀 확인 |

치트는 보스 위치를 찾아 게임을 생략하기 위한 첫 수단이 아니다. 실제 화면에
도달해야 하지만 정상 플레이 자동화가 비현실적인 경우에만, 메모리 상태와
화면 결과를 함께 기록한 제한된 검증 도구로 사용한다.

## 단계 5: 후보 판정과 릴리스

각 배치는 아래 중 하나로만 기록한다.

- PASS: 범위 검사, 부팅, 대상 바이트, 화면 문맥, 네이티브 가독성이 모두 맞다.
- FAIL: 잘못된 포인터, 부팅 실패, 글꼴 충돌, 제어 토큰 손실처럼 원인이 확인됐다.
- UNKNOWN: 정적 검사는 통과했지만 대상 화면 증거가 없다.

릴리스 후보에는 PASS인 레코드만 넣는다. 고위험 변경이나 릴리스 후보에는
수동 시각 증거를 요구하지만, 개발용 후보를 만드는 과정 자체는 막지 않는다.

## 바로 이어서 할 일

1. 영어 패치의 대사/이름표/UI/CHR 변경을 한 장의 구현 지도에 고정한다.
2. 검증된 pointer 182의 출력 타일/CHR 상태를 글꼴 컨텍스트 프로필로 고정하고,
   새 코드 풀은 그 프로필 밖에서 자동 거부하게 만든다.
3. 고정 Bank 7 글꼴만으로 표현 가능한 오프닝 대사 1-2개를 골라, 영어 패치가
   증명한 포인터 재배치 모델로 파일럿 후보를 만든다.
4. 동적 CHR 페이지는 마지막 고정 PRG 뱅크 안의 안전한 실행 공간 또는 코드
   재배치가 증명될 때까지 별도 연구로 남긴다. `FEDD`에서 Bank 1 코드 동굴로
   점프하는 방식은 금지한다.
5. 파일럿 화면이 PASS가 된 뒤에만 같은 고정 페이지를 공유하는 인접 레코드를
   다음 작은 배치로 묶는다.

따라서 앞으로의 FCEUX 실행은 오프닝을 오래 돌리는 일이 아니라, 위의 특정
레코드나 특정 화면을 확인하고 즉시 종료하는 짧은 증명 실행이 된다.
